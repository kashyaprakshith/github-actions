name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build /home/runner/work/github-actions/github-actions. --file Dockerfile --tag my-image-name
    - name: show images
      run: docker image ls
